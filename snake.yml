nome:  gerar  jogo pacman 

on: schedule: # Executar automaticamente a cada 24 horas - cron: "0 */24 * * *" workflow_dispatch: # Permite acionamento manual push: # Executa a cada push para o branch principal branches: - main
   
      
   
   
    
       

trabalhos: gerar: permissões: conteúdo: escrever execuções-em: ubuntu-mais-último tempo-limite-minutos: 5
  
    
       
     
     

    passos: - nome: gerar pacman-contribution-graph.svg usa: abozanona/pacman-contribution-graph@main com: github_user_name: ${{ github.repository_owner }}
         
         
        
             

      # Envie o SVG gerado para o branch de saída - nome: envie pacman-contribution-graph.svg para o branch de saída usa: crazy-max/ ghaction-github-pages@v3.1.0 com: target_branch: saída build_dir: dist env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
             
         
        
           
           
        
             
